import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'kiipbox-ang';
  // displayText = 'before_login';
  // visible = true;
  // constructor() { }

  // toggle() {
  //   this.visible = !this.visible;
  //   this.displayText = this.visible ? 'before_login' : 'after_login';
  // }
}
